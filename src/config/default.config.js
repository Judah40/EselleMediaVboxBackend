require("dotenv").config;

const {
  DB_HOST,
  DB_NAME,
  APP_PORT,
  DB_USERNAME,
  DB_PASSWORD,
  DB_PORT,
  APP_URL,
  JWT_SECRET,
  BUCKET_NAME,
  BUCKET_REGION,
  ACCESS_KEY,
  SECRET_ACCESS_KEY,
  TWILIO_SID,
  TWILIO_TOKEN,
  MEDIA_LIVE_INPUT_ID,
  MEDIA_LIVE_DESTINATION_ID,
  AWS_MEDIA_ROLE_ARN,
  AWS_MEDIA_PACKAGE_DESTINATION_URL1,
  AWS_MEDIA_PACKAGE_DESTINATION_USERNAME1,
  AWS_MEDIA_PACKAGE_DESTINATION_URL2,
  AWS_MEDIA_PACKAGE_DESTINATION_USERNAME2,
} = process.env;

module.exports = {
  appPort: APP_PORT,
  appUrl: APP_URL,
  jwtSecret: JWT_SECRET,
  dbUrl: DB_HOST,
  dbName: DB_NAME,
  dbPort: DB_PORT,
  dbUsername: DB_USERNAME,
  dbPassword: DB_PASSWORD,
  awsBucketName: BUCKET_NAME,
  awsBucketRegion: BUCKET_REGION,
  awsAccessKey: ACCESS_KEY,
  awsSecretAccessKey: SECRET_ACCESS_KEY,
  accountSid: TWILIO_SID, // Replace with your Twilio Account SID
  authToken: TWILIO_TOKEN,
  destinationId: MEDIA_LIVE_DESTINATION_ID,
  inputId: MEDIA_LIVE_INPUT_ID,
  roleArn: AWS_MEDIA_ROLE_ARN,
  destinationUrl1: AWS_MEDIA_PACKAGE_DESTINATION_URL1,
  destinationUsername1: AWS_MEDIA_PACKAGE_DESTINATION_USERNAME1,
  destinationUrl2: AWS_MEDIA_PACKAGE_DESTINATION_URL2,
  destinationUsername2: AWS_MEDIA_PACKAGE_DESTINATION_USERNAME2,
};
